import styled from "styled-components";

interface Props {}

export const Text = styled.span<Props>`
  margin: 0 12px;
  font-size: 14px;
  line-height: 36px;
  font-weight: 600;
  color: white;
  user-select: none;
`;
