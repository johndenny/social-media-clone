import styled from "styled-components";

interface Props {}

export const PlaceholderText = styled.div<Props>`
  height: 18px;
  width: 60px;
  background-color: rgb(var(--highlight-background));
  border-radius: 4px;
  margin: 8px auto 0 auto;
`;
