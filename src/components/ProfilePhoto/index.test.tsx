import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

// test("should pass height to height and width of span", () => {
//   render(<ProfilePicture user={} height="25px" />);
//   expect(screen.getByRole("link")).toHaveStyle("height: 25px; width: 25px;");
// });
