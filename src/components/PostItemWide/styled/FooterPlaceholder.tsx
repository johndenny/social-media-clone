import styled from "styled-components";

interface Props {}

export const FooterPlaceholder = styled.div<Props>`
  height: 150px;
  margin-top: auto;
  border-top: 1px solid rgb(var(--highlight-background));
  padding: 16px;
`;
