import styled from "styled-components";

interface Props {}

export const MorePostHeader = styled.h2<Props>`
  font-weight: 600;
  color: rgb(var(--secondary-text));
  margin: 56px 0 16px 0;
  padding-top: 44px;
  border-top: 1px solid rgb(var(--stroke));
`;
