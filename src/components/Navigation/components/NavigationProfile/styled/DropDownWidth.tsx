import styled from "styled-components";
import { DropDownLink } from "./DropDownLink";

interface Props {}

export const DropDownWidth = styled.div<Props>`
  width: 230px;
  background-color: rgb(var(--primary-background));
  border-radius: 6px;
`;
