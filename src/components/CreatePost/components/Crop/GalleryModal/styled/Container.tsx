import styled from "styled-components";

interface Props {}

export const Container = styled.div<Props>`
  width: 100%;
  padding: 8px;
  position: absolute;
  right: 0;
  bottom: 0;
  align-items: flex-end;
`;
