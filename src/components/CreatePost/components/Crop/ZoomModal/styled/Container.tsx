import styled from "styled-components";

interface Props {}

export const Container = styled.div<Props>`
  position: absolute;
  bottom: 0;
  left: 0;
  margin-left: 44px;
  padding: 8px;
  align-items: flex-start;
  z-index: 3;
`;
