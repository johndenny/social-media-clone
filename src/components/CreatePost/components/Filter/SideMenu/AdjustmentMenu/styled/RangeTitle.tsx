import styled from "styled-components";

interface Props {}

export const RangeTitle = styled.div<Props>`
  flex-direction: row;
  justify-content: space-between;
  font-size: 16px;
  padding: 16px 0;
`;
