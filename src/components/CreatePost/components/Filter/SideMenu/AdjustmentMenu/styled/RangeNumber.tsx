import styled from "styled-components";

interface Props {}

export const RangeNumber = styled.span<Props>`
  font-size: 12px;
  color: rgb(var(--secondary-text));
  width: 36px;
  text-align: right;
`;
