import styled from "styled-components";
import { Button } from "../../CommentItem/styled";

interface Props {}

export const ToggleButton = styled(Button)<Props>`
  margin-bottom: 8px;
  align-items: center;
  gap: 16px;
`;
