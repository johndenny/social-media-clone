import styled from "styled-components";

interface Props {}

export const TagButton = styled.button<Props>`
  background: rgba(0, 0, 0, 0.8);
  border-radius: 50%;
  padding: 8px;
  position: absolute;
  margin: 12px;
  bottom: 0;
`;
