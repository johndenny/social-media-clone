import styled from "styled-components";
import { ExpandSprite } from "./ExpandSprite";

interface Props {}

export const RotateSprite = styled(ExpandSprite)<Props>`
  background-position: 0 -59px;
`;
