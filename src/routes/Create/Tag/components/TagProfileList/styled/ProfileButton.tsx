import styled from "styled-components";

interface Props {}

export const ProfileButton = styled.button<Props>`
  text-align: left;
  color: rgb(var(--primary-text));
  font-weight: 600;
  font-size: 14px;
  line-height: 18px;
`;
