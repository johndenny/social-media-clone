import styled from "styled-components";

interface Props {}

export const HeaderSecondary = styled.h2<Props>`
  padding: 0 16px;
  margin-bottom: 16px;
  font-weight: 600;
  color: rgb(var(--secondary-text));
`;
