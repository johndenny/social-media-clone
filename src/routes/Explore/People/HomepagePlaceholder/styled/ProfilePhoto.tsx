import styled from "styled-components";

interface Props {}

export const ProfilePhoto = styled.div<Props>`
  height: 32px;
  width: 32px;
  background-color: rgb(var(--highlight-background));
  border-radius: 50%;
`;
