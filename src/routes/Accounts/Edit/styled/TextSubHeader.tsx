import styled from "styled-components";

interface Props {}

export const TextSubHeader = styled.h2<Props>`
  color: rgb(var(--secondary-text));
  font-size: 14px;
  font-weight: 600;
  margin-bottom: -8px;
`;
