import styled from "styled-components";

interface Props {}

export const RequestContainer = styled.div<Props>`
  flex-direction: row;
  justify-content: space-between;
  padding: 8px 16px 4px 16px;
  font-weight: 600;
`;
