import styled from "styled-components";

interface Props {}

export const UnreadIcon = styled.div<Props>`
  height: 8px;
  width: 8px;
  background-color: rgb(var(--primary-button));
  border-radius: 4px;
  align-self: center;
`;
