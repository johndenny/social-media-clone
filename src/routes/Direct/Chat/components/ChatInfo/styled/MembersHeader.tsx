import styled from "styled-components";

interface Props {}

export const MembersHeader = styled.header<Props>`
  flex-direction: row;
  justify-content: space-between;
  padding: 8px 16px;
  font-size: 16px;
  font-weight: 600;
`;
