import styled from "styled-components";

interface Props {}

export const NewPostButtonContainer = styled.div<Props>`
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  align-items: center;
  z-index: 3;
`;
